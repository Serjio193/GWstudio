# Python Runtime

Known local runtime checked during release preparation:

- Python 3.13 from `C:\Users\serji\AppData\Local\Programs\Python\Python313\`

TODO before release:

- Add the Python Software Foundation license for the exact bundled Python runtime.
- Keep bundled Python `LICENSE.txt` inside `tools.zip`.
- Add license notices for bundled Python packages, including `gnwmanager`, `pyocd`, and their dependencies.
