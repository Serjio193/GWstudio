# Git for Windows / Bash

TODO before release:

- Add license notices for the exact bundled Git for Windows package.
- Include Git, MSYS2, MinGW runtime, and related notices as shipped upstream.
- Keep package license files inside `tools.zip`.
